#include <stdio.h>

int main() {
    int category, distance;

    scanf("%d", &category);
    scanf("%d", &distance);

    switch (category) {
        case 1: // Student
            if (distance <= 30)
                printf("Eligible");
            else
                printf("Eligible with Extra Fee");
            break;

        case 2: // Employee
            if (distance <= 30)
                printf("Eligible");
            else
                printf("Not Eligible");
            break;

        default:
            if (distance <= 30)
                printf("Eligible");
            else
                printf("Eligible with Extra Fee");
    }
    return 0;
}
