#include <stdio.h>

int main() {
    int r, c;

    int arr[r][c];
    printf("enter the rows and column value:\n");
    scanf("%d %d",&r,&c);

    printf("enter the value of array element:\n");

    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            scanf("%d", &arr[i][j]);
        }
    }
    int count =0;
    for(int i=0;i<r;i++){

        for(int j=0;j<c;j++){

        if(arr [i][j]==0){
            count++;
        }
        
        }
    }

    printf("%d number of seat we had empty" , count);
    return 0;
}
