#include <stdio.h>
int main(){
    int i,n,sum=0;
    
    printf ("enter the number");
    scanf ("%d", &n);

    for (int i=1;i<=n;i++){
        int k= i*i;
        sum = sum+k;
    }
    printf ("sum of square series : %d" , sum);
    return 0;
}
