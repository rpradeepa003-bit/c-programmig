#include <stdio.h>
int main() {
    int N,heavydays,total=0, rainfall;
    printf("enter the number of days :");
    scanf("%d", &N);

    for(int i=1;i<= N;i++){
        printf("enter the rainfall per day  %d :", i);
            scanf("%d", &rainfall);
            total = total + rainfall; 

        if(rainfall > 50) {
            heavydays++;           
        }
    }    
    printf("total rainfall = %d\n", total);
    printf ("heavy rainy days = %d\n", heavydays);
    return 0;
}
