#include <stdio.h>

int main() {
    int i, n;

    printf("Enter the value of N (1 to 1000): ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
            printf("%d\n", i);

    }

    return 0;
}
