#include<stdio.h>
int main(){
    int n;
    int reverse=0;
    scanf("%d",&n);
    while(n>0){
        int digit=n%10;
        reverse =reverse*10+digit;
        n = n/10;
    }
    printf("%d",reverse);
    return 0;
}
