#include<stdio.h>
int main(){
    int n,even;
    printf("enter a numbers:");
    scanf("%d",&n);
    int i=1;
    while(i<=n){
        if(i%2==0){
          printf ("%d ",i);
   
        }
         i++;  
        }
    return 0;
}
