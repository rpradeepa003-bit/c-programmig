#include <stdio.h>

int main() {
    int n, count = 0;
    int a = 0, b = 1, next;

    scanf("%d", &n);

    while(count < n) {
        printf("%d ", a);
        next = a + b;
        a = b;
        b = next;
        count++;
    }

    return 0;
}
