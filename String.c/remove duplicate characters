#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    int i, j;
    int visited[256] = {0};

    scanf("%s", str);

    for(i = 0; str[i] != '\0'; i++) {
        if(visited[str[i]] == 0) {
            printf("%c", str[i]);
            visited[str[i]] = 1;
        }
    }

    return 0;
}
